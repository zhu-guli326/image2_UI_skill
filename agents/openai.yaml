interface:
  display_name: "Image2 UI 复刻"
  short_description: "把 UI 参考图转成可点击 demo，并接入 imagegen 优先的 image2 资产"
  default_prompt: "Use $image-to-ui-skill 分析这些 UI 参考图，判断代码 UI 与 image2 资产边界，默认先走本地 imagegen 生成图片资产；如果本地 imagegen 不可用或失败，再走 scripts/image2_asset.py 本地 API fallback，并把生成资产集成回界面，最后交付可点击预览和截图验真。"
